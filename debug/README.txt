-Goal:
R scripts in this folder are used to check parameters individually.
-Results:
Every parameter is close to the true value if we sample from its posterior distribution when setting other parameters equal to their true values. 