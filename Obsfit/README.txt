As of 8/20/2017:

-f1(Study 11): 50% MCAR, initial value of parameters are close to true values, fit models only using observed data

-f2(Study 12): 50% MCAR, initial value of parameters are not close to true values, fit models only using observed data

-f3(Study 13): MAR( depends on V_{i} and Y_{it}), initial values are close to true values. fit models only using observed data. 

-f4(Study 14): MAR( depends on age and gender), initial values are close to true values. fit models only using observed data. 

-f5(Study 15): MAR( depends on V_{i} and Y_{it}), initial values are not close to true values. fit models only using observed data. 

-f6(Study 16): MAR( depends on age and gender), initial values are not close to true values. fit models only using observed data. 
